import { render } from "@testing-library/react";
import NewReelForm from "./NewReelForm";
//Todo: Add tests
describe("NewReelForm", () => {
  //Test reel name validation (if any)
  test("Renders NewReelForm", () => {
    //Arrange
    //Act
    //Assert
  });
  //Test reel name validation (if any)
  test("Reel name validation", () => {
    //Arrange
    //Act
    //Assert
  });

  test("Select Standard - PAL is checked, NTSC is unchecked", () => {
    //Arrange
    //Act
    //Assert
  });

  test("Select Standard - NTSC is checked, PAL is unchecked", () => {
    //Arrange
    //Act
    //Assert
  });
  test("Select Definition - SD, HD is unchecked", () => {
    //Arrange
    //Act
    //Assert
  });

  test("Select Definition - HD, SD is unchecked", () => {
    //Arrange
    //Act
    //Assert
  });
});
