import { render, screen } from "@testing-library/react";
import ReelItem from "./ReelItem";
//Todo: Add tests
describe("ReelItem", () => {
  test("renders ReelItem", () => {
    //Arrange
    //Assert
  });
});
